// Package types defines shared types used across the chaosseed-core engine.
//
// This package has no dependencies on other chaosseed packages.
// All other packages may depend on types, but types must not import them.
package types

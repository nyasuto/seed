name: Coverage

on:
  push:
    branches: [main]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: stable

      - name: Run tests with coverage (core)
        run: cd core && go test -coverprofile=coverage.out ./...

      - name: Check coverage threshold
        run: |
          COVERAGE=$(cd core && go tool cover -func=coverage.out | grep total | awk '{print $3}' | sed 's/%//')
          echo "Total coverage: ${COVERAGE}%"
          THRESHOLD=80.0
          if echo "$COVERAGE $THRESHOLD" | awk '{exit !($1 < $2)}'; then
            echo "::warning::Coverage ${COVERAGE}% is below ${THRESHOLD}% threshold"
          fi

// Package server provides a high-level game server that wraps core's
// SimulationEngine and exposes an ActionProvider-based interface for
// driving game execution. It bridges external action sources (human UI,
// AI agents, batch runners) to the core simulation loop.
package server
